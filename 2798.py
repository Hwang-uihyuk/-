res = 0

n,m = map(int,input().split())
card = list(map(int,input().split()))
card.sort()
for i in range(1,len(card)-1):
    for j in range(i+1,len(card)):
        for k in range(j+1,len(card)+1):
           if res = card[i]+card[j]+card[k]
            
